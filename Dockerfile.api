# Dockerfile.api
FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci --legacy-peer-deps --omit=dev

# Copy source code
COPY . .

# Expose API port
EXPOSE 3000

# Start the API
CMD ["npm", "start"]
